info:
  name: get updates
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/connection/uni_sse?cf_connect={{sseToken}}"
  params:
    - name: cf_connect
      value: "{{sseToken}}"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
