info:
  name: send vote
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/polls/:pollSlug/vote"
  params:
    - name: pollSlug
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "options": [28, 29]
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
