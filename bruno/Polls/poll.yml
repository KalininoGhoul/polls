info:
  name: poll
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/polls/:pollSlug"
  params:
    - name: pollSlug
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
