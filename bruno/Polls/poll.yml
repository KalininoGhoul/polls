info:
  name: poll
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/polls/:pollSlug"
  params:
    - name: pollSlug
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
