fastcgi_cache_path /tmp/nginx_cache levels=1:2 keys_zone=fastcgicache:10m inactive=70m max_size=${FASTCGI_CACHE_MAX_SIZE};
fastcgi_cache_key "$request_method$host$request_uri";

map $request_method $no_cache_request_method {
    default 1;
    GET 0;
    HEAD 0;
}
